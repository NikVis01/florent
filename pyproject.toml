[project]
name = "florent"
version = "0.1.0"
description = "Business graph modeling and traversal"
dependencies = [
    "pydantic>=2.0.0",
    "dspy-ai>=2.0.0",
    "structlog>=25.5.0",
]

[tool.uv]
package = true
dev-dependencies = [
    "ruff>=0.1.0",
]
